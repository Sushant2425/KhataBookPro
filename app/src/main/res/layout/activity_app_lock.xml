<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F8F9FA">

    <!-- Toolbar with Back Arrow (WHITE ARROW GUARANTEED) -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@drawable/dashboard_header_background"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:title="App Lock Settings"
        app:titleTextColor="@android:color/white"
        app:navigationIcon="?attr/homeAsUpIndicator"
        app:navigationIconTint="@android:color/white" />

    <!-- Main Content Card -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="8dp"
        app:cardBackgroundColor="@android:color/white">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <!-- Icon + Title -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginBottom="24dp">

                <ImageView
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:src="@drawable/ic_lock_outline"
                    android:tint="#1A73E8"
                    android:layout_marginEnd="16dp"/>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="App Lock Protection"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    android:textColor="#202124" />

            </LinearLayout>

            <!-- Description -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Secure your app with fingerprint, face recognition, or device PIN/pattern/password."
                android:textColor="#5F6368"
                android:textSize="14sp"
                android:layout_marginBottom="32dp"/>

            <!-- App Lock Switch -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingVertical="12dp">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Enable App Lock"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="#202124"/>

                <androidx.appcompat.widget.SwitchCompat
                    android:id="@+id/switch_applock"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:checked="false" />

            </LinearLayout>

            <!-- Status Text -->
            <TextView
                android:id="@+id/tv_status_applock"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Status: Disabled"
                android:textSize="15sp"
                android:textColor="#1A73E8"
                android:layout_marginTop="12dp"
                android:padding="12dp"
                android:background="#E8F0FE"
                android:gravity="center"
                android:textStyle="bold"/>

        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- Note at Bottom -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Note: You will be asked to authenticate every time you open the app if App Lock is enabled."
        android:textColor="#757575"
        android:textSize="13sp"
        android:padding="20dp"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="24dp"
        android:gravity="center"
        android:background="#FFF8E1"
        android:layout_marginHorizontal="20dp"
        android:paddingVertical="16dp"
        android:paddingHorizontal="20dp"
        android:elevation="2dp"/>

</LinearLayout>